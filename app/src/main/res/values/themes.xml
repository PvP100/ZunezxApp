<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ZunezxApp" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">#8C0000</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Progressbar">
        <item name="android:indeterminateOnly">true</item>
        <item name="android:progressDrawable">@drawable/custom_progess_bar</item>
        <item name="android:indeterminateDrawable">@drawable/custom_progess_bar</item>
    </style>
</resources>